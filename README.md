# Smart Task Analyzer

## Overview
Smart Task Analyzer scores and prioritizes tasks using urgency, importance, effort, and dependencies.
This repository contains a Django backend with two endpoints and a simple frontend (static files).

## Running locally

1. Create virtualenv and install dependencies:
```bash
cd backend
python -m venv venv
source venv/bin/activate      # Windows: venv\Scripts\activate
pip install -r requirements.txt
